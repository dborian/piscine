#include <stdio.h>
#include <unistd.h>

char	ft_strupcase(char *str)
{
	int i = 0;
	while(str[i] != '\0')
	{
		str[i] = str[i]-20;
	}
	return(str);
}
int	main()
{
	char str[] = "salut";
	printf("src = %s \ndest = %s\n", src, dest);
	printf("str = %s",ft_strupcase(str));
	return(0);
}
