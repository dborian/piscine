/*
** COBRA PROJECT, 2023
** cesar_code
** File description:
** Exercice 1 -  Etape 1 : Dechiffrement
*/

#include <stdio.h>
#define IS_LOWER(x) ((x) >= 'a' && (x) <= 'z')
#define IS_UPPER(x) ((x) >= 'A' && (x) <= 'Z')
#define IS_ALPHA(x) (IS_LOWER(x) || IS_UPPER(x))

void dechiffrement(char *str, int key)
{
    (void)str; (void)key; // <== Pensez à supprimer cette ligne avant de commencer l'exercice
}
